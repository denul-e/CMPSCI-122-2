#include <stdio.h>

int main() {
    const int limit = 100;
    int num;

    // Prompt the user for a number
    printf("Enter a number: ");
    scanf("%d", &num);

    // Check if the number is high or low
    if (num >= limit) {
        printf("high\n");
    } else {
        printf("low\n");
    }

    return 0;
}

