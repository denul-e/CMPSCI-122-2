#include <iostream>
using namespace std;

int main() {
    const int limit = 100;
    int num;

    // Prompt the user for a number
    cout << "Enter a number: ";
    cin >> num;

    // Check if the number is high or low
    if (num >= limit) {
        cout << "high" << endl;
    } else {
        cout << "low" << endl;
    }

    return 0;
}

